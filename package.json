{
    "name": "nexusfork",
    "author": "NexusTools",
    "homepage": "https://nexustools.com/",
    "version": "0.4.1",
    "license": "Apache-2.0",
    "description": "Easily setup a cluster based http server using express and vhost. Includes patches for specific hosts like OpenShift.",
    "keywords": [
        "hosts",
        "virtual",
        "cluster",
        "website",
        "hosting",
        "modular",
        "openshift",
        "NexusTools",
        "express",
        "vhost"
    ],
    "bugs": {
        "url": "https://github.com/NexusTools/node-nexusfork/issues"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/NexusTools/node-nexusfork.git"
    },
    "preferGlobal": true,
    "main": "src/master.js",
    "scripts": {
        "test": "mocha --bail --require ./test/support/env --reporter spec --timeout 1000000 --globals __typeinclude__ --check-leaks test/",
        "test-coverage": "istanbul cover node_modules/mocha/bin/_mocha -- --timeout 20000 --require test/support/env --bail --reporter spec --globals __typeinclude__ --check-leaks test/",
        "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --timeout 20000 --require test/support/env --reporter spec --globals __typeinclude__ --check-leaks test/"
    },
    "engines": {
        "node": ">=0.11.16",
        "npm": ">=1.3.8"
    },
    "dependencies": {
        "argwrap": "^0.0.2",
        "express": "^4.15.2",
        "nulllogger": "^0.3.0",
        "node-paths": "^0.2.0",
        "compression": "^1.6.2",
        "chokidar": "^1.6.1",
        "lodash": "^4.17.4",
        "vhost": "^3.0.2"
    },
    "devDependencies": {
        "istanbul": "^0.4.5",
        "mocha": "^3.2.0"
    }
}
